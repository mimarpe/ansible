- apiVersion: route.openshift.io/v1
  kind: Route
  metadata:
    annotations:
    labels:
      app: {{appname}}-grafana
    name: {{appname}}-grafana
  spec:
    host: grafana-{{appname}}.{{oc_endpoint[env_domain]}}
    port:
      targetPort: http
    tls:
      termination: edge
    to:
      kind: Service
      name: {{appname}}-grafana
    wildcardPolicy: None

