- apiVersion: route.openshift.io/v1
  kind: Route
  metadata:
    annotations:
    labels:
      app: {{appname}}-blackbox
    name: {{appname}}-blackbox
  spec:
    host: blackbox-{{appname}}.{{oc_endpoint[env_domain]}}
    port:
      targetPort: http
    tls:
      termination: edge
    to:
      kind: Service
      name: {{appname}}-blackbox
    wildcardPolicy: None

