- apiVersion: route.openshift.io/v1
  kind: Route
  metadata:
    annotations:
    labels:
      app: {{appname}}-prometheus
    name: {{appname}}-prometheus
  spec:
    host: prometheus-{{appname}}.{{oc_endpoint[env_domain]}}
    port:
      targetPort: http
    tls:
      termination: edge
    to:
      kind: Service
      name: {{appname}}-prometheus
    wildcardPolicy: None

